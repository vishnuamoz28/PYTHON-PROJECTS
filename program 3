string = raw_input("Enter a string : ")
words = string.split(" ")
d = dict()
for w in words:
    if w in d:
        d[w] += 1
    else:
        d[w] = 1
for x in d.keys():
    print("%s occurs %s times." % (x, d[x]))
